extends ./templates/layout

block variables
  -
    var appId = 'top';  // JSのアプリ起動に使われるID名（使わない場合は空文字）
    var title = 'トップ';
    var description = '説明';

block content
  p test
  p.test__title タイトル
